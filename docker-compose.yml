version: '3'
services:
 
  gvm:
    image: falkowich/gvm-docker:dev
    ports: 
      - 443:443
      - 9391:9391
        #    restart: never
    volumes:
      - gvm:/usr/local/var/lib/gvm/
      - psql:/var/lib/postgresql/
volumes:
  gvm:
  psql:
